{
  "issues": [
    {
      "name": "Permission-scoped OATs + role→permission model (v2 of #43)",
      "id_number": 44,
      "description": "Evolve Organization Access Tokens (shipped role-scoped in #43) to carry a set of PERMISSIONS (app-defined atomic operations, e.g. `endpoint:revise`) instead of org RBAC role slugs. This is permission-based access control (PBAC) layered over the existing RBAC, matching GitHub fine-grained PATs / Stripe restricted keys.\n\nPRINCIPLE: authkit stays permission-AGNOSTIC. It still hardcodes only the `owner` role; the permission catalog, roles, and role→permission map all live in the consuming app (tensorhub, see its issue). authkit just (a) carries opaque permission strings on an OAT, (b) exposes them in claims, and (c) delegates mint authorization to a host hook.\n\nCLAIMS SHAPE (what middleware produces):\n  - OAT principal:  { Org, Permissions:[...], TokenType:'service' }  (no UserID, no roles)\n  - User principal: { Org, UserID, OrgRoles:[...] }  (Permissions stays EMPTY — the resource server expands roles→permissions at request time; do NOT bake a permission union into the JWT: it goes stale when role definitions change and bloats the token)\n\nMINT AUTHORIZATION via a host hook (authkit can't interpret app permissions): authkit calls a host-provided OATGrantAuthorizer at POST /orgs/{org}/access-tokens to answer BOTH 'may this caller mint?' and 'may they grant these permissions?'. tensorhub implements it by checking the caller's effective permissions (from their roles) ⊇ the requested set, and that the caller holds an `oat:mint` permission. When no authorizer is configured, fall back to the #43 behavior (owner-only mint). The role-subset no-escalation check moves OUT of authkit (it no longer knows what permissions mean) and INTO the hook; keep the structural guard that a service principal (an OAT) can never reach the mint handler.\n\nDepends on nothing; consumed by tensorhub #369 and e2e #94.",
      "tasks": [
        "[x] Add `Claims.Permissions []string`; middleware sets it for OAT principals (repoint from the #43 OrgRoles reuse). User principals keep roles in OrgRoles, Permissions empty.",
        "[x] Rename OAT `scopes`→`permissions` across MintOrgAccessToken/List/Resolve, the request/response JSON, and the `org_access_tokens` column (migration: rename column `scopes`→`permissions`). Values are opaque app permission strings.",
        "[x] Define `OATGrantAuthorizer` interface { CanGrantOAT(ctx, caller, org string, permissions []string) (allowed bool, offending []string, err error) } + `WithOATGrantAuthorizer` Service option.",
        "[x] POST /orgs/{org}/access-tokens: call the authorizer for mint authority + permission grant; owner-only fallback when none configured. Return 403 with offending permissions named on denial.",
        "[x] Remove the org-role-subset no-escalation check from the handler (now the hook's job); keep service-principal-cannot-mint guard.",
        "[x] Tests: hook allow/deny (offending named), owner-only fallback, Claims.Permissions populated for OATs, OAT round-trip; update #43 tests for permissions naming.",
        "[x] Docs: api-endpoints.md + README OAT section (permission-scoped; hook contract; permissions vs roles).",
        "[x] Version bump notes; flag consumer follow-ups (tensorhub #369, e2e #94)."
      ],
      "completed": true
    },
    {
      "name": "Passkey (WebAuthn/FIDO2) authentication — register, login, manage",
      "id_number": 45,
      "description": "Add passkeys (WebAuthn/FIDO2) as a first-class authentication method in authkit, alongside password, OIDC, and SIWS. Passkeys are phishing-resistant, usernameless-capable credentials bound to the relying party (RP) domain. A user can register one or more passkeys and authenticate with them; a successful login mints the SAME access/refresh session as the password path (and honors the optional `org` body param).\n\nLIBRARY: github.com/go-webauthn/webauthn for ceremony options + attestation/assertion verification. authkit owns storage, ephemeral challenge handling, session minting, routing, policy.\n\nRP CONFIG (host-provided, on core.Config): RPID (registrable domain), RPDisplayName, allowed Origins. Derive defaults from BaseURL/Issuer; validate RPID is a registrable suffix of each origin.\n\nCEREMONIES (begin -> finish; challenge state in the EphemeralStore, same pattern as SIWS challenges + reset tokens, short-TTL single-use): REGISTRATION (AUTH'd user) begin->CreationOptions (challenge, RP, per-user handle, excludeCredentials, residentKey=preferred) + finish (verify attestation, store credential). AUTHENTICATION (login) begin->RequestOptions supporting BOTH discoverable/usernameless AND username-scoped (prefer discoverable) + finish (verify assertion, sign-count clone detection, update sign_count/last_used, mint session).\n\nSTORAGE: new profiles.user_passkeys (id uuidv7, user_id fk, credential_id bytea UNIQUE, public_key bytea, sign_count bigint, aaguid bytea, transports text[], attestation_fmt text, label, created_at, last_used_at, deleted_at). A per-user random user_handle (NOT the user id) maps handle->user for usernameless login.\n\nSECURITY: RPID/origin phishing-resistance (library-enforced); sign-count regression -> reject (clone); single-use short-TTL challenges; anti-enumeration on username-scoped login begin; rate-limit begin+finish; live-user ban/deleted gate on login.\n\nMIGRATION PACKAGING (do it right): add profiles.user_passkeys as a NEW NUMBERED migration (002_user_passkeys.up.sql), NOT appended to the consolidated 001 file — migratekit is name-tracked and won't re-apply 001 to DBs that already recorded it, so tables added to 001 never reach existing deployments (the org_access_tokens gotcha). A new numbered file IS applied to existing DBs.\n\nROUTES (new RouteGroup RoutePasskeys): POST /passkeys/register/begin (AUTH), POST /passkeys/register/finish (AUTH), POST /passkeys/login/begin (PUBLIC), POST /passkeys/login/finish (PUBLIC), GET /passkeys (AUTH; metadata only), DELETE /passkeys/{id} (AUTH), PATCH /passkeys/{id} rename (AUTH, optional).\n\nNON-GOALS: enterprise/attestation-conveyance policy (accept 'none'); MDS metadata validation; account recovery when all passkeys are lost (rely on existing password/email recovery).",
      "tasks": [
        "[ ] Add go-webauthn dep; WebAuthn RP config on core.Config (RPID, RPDisplayName, Origins) + BaseURL-derived defaults + validation (RPID a registrable suffix of each origin).",
        "[ ] NEW numbered migration 002_user_passkeys.up.sql: profiles.user_passkeys + indexes (unique credential_id, index user_id). Do NOT append to 001.",
        "[ ] Storage: CRUD for user_passkeys (create/list-by-user/get-by-credential-id/update-sign-count+last-used/soft-delete) + per-user user_handle generation & handle->user lookup.",
        "[ ] Registration ceremony: begin (CreationOptions, excludeCredentials, ephemeral single-use challenge) + finish (verify attestation, persist). AUTH-gated.",
        "[ ] Authentication ceremony: begin (discoverable + username-scoped, anti-enumeration) + finish (verify assertion, sign-count clone-check, update sign_count/last_used, mint access+refresh honoring `org`, live-user gate).",
        "[ ] Management routes: GET /passkeys (metadata only), DELETE /passkeys/{id}, optional PATCH rename.",
        "[ ] RouteGroup RoutePasskeys + registration; challenge state via EphemeralStore (single-use, short TTL) like SIWS.",
        "[ ] Rate-limit buckets for register/login begin+finish; anti-enumeration on username-scoped login begin.",
        "[ ] Tests: full register + login ceremonies via a software-authenticator fixture; sign-count regression rejection; usernameless login; list/delete; anti-enumeration; rate limits.",
        "[ ] Docs: api-endpoints.md + README passkey section (RP config, ceremony flow, frontend navigator.credentials notes, security model, recovery out-of-scope).",
        "[ ] Version bump + publish; consumer notes (host mounts RoutePasskeys + sets RP config; frontend integrates the WebAuthn JS ceremonies)."
      ],
      "completed": false
    },
    {
      "name": "Org RBAC engine in authkit: base permissions + app catalog/default-roles + generic role->permission management (+ removes OAT hook)",
      "id_number": 46,
      "description": "authkit is the COMPLETE, GENERIC org-RBAC engine. The embedding app declares its own permission CATALOG; authkit ALSO ships a small set of BASE permissions for org-management itself. The full catalog an org sees = authkit-base permissions UNION the app-declared catalog. authkit manages everything generically over opaque strings (role->permission CRUD, per-member assignment, effective-permission computation, no-escalation, OATs); its only catalog job is set-membership validation ('is this permission defined?').\n\nBASE PERMISSIONS (built into authkit, reserved `org:` namespace, apps may NOT redefine):\n- org:roles:manage  -> create/modify/delete roles + set a role's permission set\n- org:members:manage -> add/remove org members + grant/remove their roles\n- org:tokens:manage -> mint/revoke OATs\n- org:read (optional) -> view members/roles/tokens\nThese gate authkit's OWN org-management endpoints via the SAME permission system (replacing the current hardcoded requireOrgOwner). So all authz is uniform/permission-based.\n\nAPP CATALOG (declared at init): core.Config.PermissionCatalog []PermissionDef{Name,Description} for app-specific permissions (tensorhub: endpoint:deploy, repo:write, secrets:write, dataset:write, ...). Merged with the base set. Opaque to authkit.\n\nROLES: `owner` is built-in = `*` (= base UNION app; the only role holding org:*:manage by default -> effectively owner-exclusive management). The APP can declare DEFAULT ROLE TEMPLATES (name + permission set) seeded into every org at creation alongside owner. tensorhub declares `admin` = owner MINUS {org:roles:manage, org:members:manage} (admin does everything else incl. mint OATs + all app perms, but cannot manage roles or membership). Represent 'owner minus X' via `*` + an exclusion list, or enumerate; decide.\n\nDATA: NEW numbered migration (NOT appended to 001 — migratekit name-tracking gotcha): profiles.org_role_permissions (org_id, role, permission; FK (org_id,role)->org_roles ON DELETE CASCADE; UNIQUE). Seed owner=`*`; seed app-declared default roles per org.\n\nEFFECTIVE PERMISSIONS: core.EffectivePermissions(ctx, org, userID) = union over the member's roles (`*` => superset). Exposed for embedding-app request-time enforcement (tensorhub #369 reads this) + admin HTTP GET. Never baked into the JWT (staleness).\n\nVALIDATION (generic, over base UNION app catalog) on role-assignment AND OAT mint: permission must be in the catalog (else unknown_permission) AND within the actor's effective permissions (else no-escalation 403 + offending). OATs are NEVER grantable org:*:manage and can't reach management endpoints (handlers require a real user) — an OAT does machine work, never org management.\n\nOAT SIMPLIFICATION (supersedes part of #44): authkit holds the catalog + computes effective perms, so it enforces OAT-mint validation + no-escalation ITSELF — REMOVE the tensorhub OATGrantAuthorizer hook (CanGrantOAT/WithOATGrantAuthorizer); tensorhub drops oat_grant.go + the wiring.\n\nROUTES (permission-gated): GET /permissions (catalog = base UNION app); GET/PUT(+optional POST/DELETE) /orgs/{org}/roles/{role}/permissions (org:roles:manage); GET /orgs/{org}/members/{user_id}/permissions (effective). Existing member/role/OAT management endpoints re-gated onto org:members:manage / org:roles:manage / org:tokens:manage.\n\nBOUNDARY: app's only jobs = declare its catalog + default roles (config) and ENFORCE app permissions at its own endpoints. Everything org-management is authkit, generic + reusable.\n\nNON-GOALS: permission MEANING (app-side at enforcement); global/cross-org permissions; WORKER-CAPABILITY tokens — a tensorhub<->gen-orchestrator trust boundary, NOT authkit's concern; authkit's only role there is signature/JWKS verification. Do not model worker scopes / on_behalf_of / job_id in the RBAC engine.\n\nRESOURCE-SCOPED GRANTS (from tensorhub per-resource scoping): app permission strings may carry a resource qualifier '<resource>:<action>:<name>' (e.g. repo:write:my-model, endpoint:invoke:my-llm) so a token grants only one named resource. authkit treats these opaquely, but its catalog-validation + no-escalation must operate on the BASE '<resource>:<action>' prefix: a scoped grant is valid iff its prefix is in the catalog, and no-escalation passes iff the actor holds the org-wide base perm OR the identical scoped perm. (Matching the scope to a specific resource at request time is the app's job, not authkit's.)\n\nMEMBERSHIP: 'org membership' collapses into role-holding — a user has permissions in an org iff they hold >=1 role there. core.EffectivePermissions returns the EMPTY set for a user with no roles, so consumers need no separate membership gate. tensorhub (#369) is REMOVING its org-membership concept entirely (no IsOrgMember boolean, no RequireOwnerMembership) and authorizing purely on effective permissions; authkit should expose roles/effective-perms for an (org,user), not a membership boolean.\n\nPERSONAL ORG NOTE: a 'personal org' is a NAMESPACE-collision reservation — a user `foo` and an org `foo` share one slug namespace and cannot both exist. It is NOT single-member: a personal org CAN have multiple members, so it correctly seeds the full DefaultRoles (admin/member/deployer/viewer), same as any org. The seeding bug is only the reserved-namespace CLAIM path (ClaimOrgNamespace) seeding nothing.",
      "tasks": [
        "[x] Define authkit BASE permissions (org:roles:manage, org:members:manage, org:tokens:manage, optional org:read) in a reserved `org:` namespace; merge into every org's effective catalog; reject app catalogs that redefine reserved names.",
        "[x] core.Config.PermissionCatalog ([]PermissionDef{Name,Description}) for app permissions + app-declared DEFAULT ROLE TEMPLATES (name -> permission set, e.g. admin). Validate; opaque storage.",
        "[x] NEW numbered migration profiles.org_role_permissions (+ indexes, FK to org_roles). Seed owner=`*` and the app default roles on org creation (extend CreateOrg + personal-org path).",
        "[x] core: role->permission CRUD + EffectivePermissions(ctx, org, userID) (union; `*` superset; represent owner-minus-X for admin).",
        "[x] Generic VALIDATION (role-assignment + OAT mint): in catalog (else unknown_permission) AND within actor's effective perms (else no-escalation 403 + offending). Bar org:*:manage from OAT grants.",
        "[x] HARDCUT (no legacy owner-check fallback): re-gate /orgs/{org}/members (GET=org:read, POST/DELETE=org:members:manage), /orgs/{org}/members/{user_id}/roles (GET=org:read, POST assign + DELETE unassign = org:members:manage), /orgs/{org}/roles (GET=org:read, POST/DELETE=org:roles:manage), and /orgs/{org}/invites (GET=org:read, POST/revoke=org:members:manage) from requireOrgOwner -> requireOrgPermissionGin(base perm). Role ASSIGNMENT (POST member-roles) adds no-escalation: the role's effective permissions must be a subset of the assigner's (via EffectiveRolePermissions + ValidateGrant); owner=`*`/global-admin bypass. Leave requireOrgOwner ONLY for genuinely owner-level ops with no base perm (org rename, federated-issuer register).",
        "[x] REMOVE OAT host-hook: fold CanGrantOAT into authkit's generic check; delete authhttp.OATGrantAuthorizer/OATGrantCaller/WithOATGrantAuthorizer; tensorhub drops oat_grant.go + RegisterOATGrantAuthorizer.",
        "[x] Routes: GET /permissions; GET/PUT(+optional POST/DELETE) /orgs/{org}/roles/{role}/permissions; GET /orgs/{org}/members/{user_id}/permissions.",
        "[x] Tests: base-perm gating of management endpoints (owner allowed; member w/o perm 403; member w/ org:members:manage can manage members but not roles); catalog validation; assignment no-escalation; admin default role = owner-minus-{roles,members} behaves; effective-perm union; OAT mint via generic check.",
        "[x] Docs: api-endpoints.md + README 'org RBAC' (base permissions, app catalog + default roles, owner=`*`, permission-gated management, OAT now hookless).",
        "[x] Release in the v0.11.x line (e.g. v0.11.3 — NOT v0.12.0) + tensorhub bump; consumer follow-ups: tensorhub declares its catalog + `admin` default role, reads core.EffectivePermissions in #369, removes the OAT adapter/hook; tensorhub also REMOVES its org-membership concept (RequireOwnerMembership / IsOrgMember gate) and switches to permission checks via EffectivePermissions.",
        "[x] Resource-scoped grants: ValidateGrant (OAT-mint + role-assign) now validates a concrete token against the catalog by EXACT match OR, for a 3-segment '<resource>:<action>:<name>' scoped grant, by its '<resource>:<action>' base (e.g. repo:write:my-model -> repo:write). No-escalation passes if the actor holds the exact scoped perm OR its base. Reserved 3-seg base perms (org:roles:manage) still match exactly and aren't re-split. Test: TestValidateGrant_ResourceScopedPrefix. (Was marked done in v0.11.3 but the live e2e proved it rejected repo:read:alpha as unknown_permission — now actually fixed.)",
        "[x] Membership-free model: EffectivePermissions/roles lookup returns empty for a user with no roles (no membership boolean). Document that consumers drop their separate org-membership gate and authorize purely on effective permissions.",
        "[x] FIXED: ClaimOrgNamespace (claimParkedOrgToRegistered) now seeds the owner role row + owner=* permission before assigning the owner (idempotent), so a claimed reserved namespace (e.g. tensorhub `root`) gets real owner permissions instead of nothing. Covers all claim paths (park/restricted/create-then-claim).",
        "[x] LAZY default-role seeding (decided lazy): seedRolePermissionDefaults now seeds ONLY owner=* at org creation/claim; app DefaultRoles (admin/member/deployer/viewer) are materialized LAZILY via new materializeDefaultRole the first time a role is granted (AssignRole), which also creates the org_roles row the assignment requires. Solo orgs carry no dormant role scaffolding; teammate roles appear when actually granted.",
        "[ ] RELEASE: these fixes are post-v0.11.3 — cut authkit v0.11.4, bump tensorhub's go.mod, rebuild the e2e stack. (Until then tensorhub builds against v0.11.3 and won't have the prefix-validation / claim-seeding / lazy-seeding fixes.) NOTE: a separate TENSORHUB gap blocks the e2e OAT boundary run regardless — tensorhub returns 401 for OAT bearers (service-principal classification not wired into auth_any.go request path)."
      ],
      "completed": true,
      "status": "DONE. authkit RBAC engine shipped as v0.11.3 (committed+tagged+pushed): base perms (org:roles:manage/members:manage/tokens:manage/read) + app catalog/default-roles config + migration 002 + role->perm CRUD + EffectivePermissions + ValidateGrant + seeding owner=* + management routes + HARDCUT permission-gating of all org-management endpoints + role-assignment no-escalation + OAT hook removed (native validation). Docs updated (api-endpoints.md + README). tensorhub consumer migrated (UNCOMMITTED, builds + full suite green on v0.11.3): deleted oat_grant.go + RegisterOATGrantAuthorizer wiring, declared its resource-permission catalog + the `admin` default role (= * minus org:roles:manage/org:members:manage). NOTE: tensorhub's internal/authz still enforces resource ops via its OWN RoleMap + owner:manage/oat:mint names; switching that to read authkit core.EffectivePermissions + the org: base names is #369's enforcement scope, not #46. FOLLOW-UP (same session, pre-release): made org:read OAT-grantable (IsOATGrantableReservedPermission allowlist; write/mint org:* still barred). Added introspection + REST consolidation routes: GET /orgs/{org}/me/permissions|roles (self-read, membership only, no org:read), POST /orgs/{org}/permissions/check (testIamPermissions-style {granted[]}; self or user_id w/ org:read; global-admin=all), GET /orgs/{org}/permissions/grantable (no-escalation subset + can_grant_all; ?for=oat applies OAT bars), GET /orgs/{org}/roles/{role} (single-role detail). HARDCUT route moves (no legacy): DELETE /orgs/{org}/roles/{role} and DELETE /orgs/{org}/members/{user_id} now path-param (was DELETE-with-body). New core.GrantablePermissions helper. Full suite green on pg18; docs (api-endpoints.md + README) updated. NOT yet released — pending v0.11.4 tag + tensorhub/e2e consumer bump. NOTE: GET /orgs/{org}/access-tokens is gated org:tokens:manage (NOT org:read), so an org:read-only OAT cannot enumerate tokens. CONSOLIDATION (same session, pre-release): role CRUD collapsed — PUT /orgs/{org}/roles/{role} {permissions[]} is idempotent create-or-replace (replaces POST /roles + GET/PUT /roles/{role}/permissions); GET/DELETE /roles/{role} kept. /me/permissions + /me/roles merged into GET /orgs/{org}/me -> {roles,permissions}. Dropped /permissions/grantable (+ core.GrantablePermissions) as derivable client-side. Invitee routes /org-invites -> top-level /me/invites (kept cross-org, NOT org-scoped — invitee isn't a member yet; deviated from the literal '/orgs/{org}/me/invites' ask because it'd break cross-org listing). Full suite green; docs (api-endpoints.md + README) updated. Final org-RBAC surface ready for v0.11.4."
    }
  ],
  "completed_features": [
    {
      "name": "Sign In With Solana (SIWS)",
      "completed_at": "2025-12-04",
      "summary": "Implemented SIWS authentication with challenge/login/link endpoints, Ed25519 signature verification, and nonce-based replay protection."
    }
  ]
}
